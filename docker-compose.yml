version: '3'

services:
  postgres:
    ports:
      - 5432
    image: postgres:15
    container_name: 'test-postgres'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: TyBaranina01
      POSTGRES_DB: testDB
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    depends_on:
      - postgres
    container_name: 'test'
    ports:
      - 8080:8080
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/testDB
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: TyBaranina01

